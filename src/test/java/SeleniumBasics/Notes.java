package SeleniumBasics;

public class Notes {
	//difference betwen get()and navigate().to
	//no diffference in there.both are for opening browsers
	 //ans: get()--wiil navigate to the page and wait till page load
	//only string parameter using in this
	//navigate().to--will navigate to page but doesnot wait to page load
	//string and Url instance also as parameters 

	
	//locators:8
	//what are the locators
}
